#include<bits/stdc++.h>
using namespace std;

#define ll long long 

void solve(){
    ll n;
    ll a[n];
    for(ll i = 0; i < n; i++) cin >> a[i];
     
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    ll t;
    cin >> t;
    while(t--){
        solve();
    }
}